// Package state persists the user's answers — the one file that is the
// source of truth for every run (DESIGN.md §1). Compose and .env are
// regenerated artifacts; this is what they are regenerated from.
package state
