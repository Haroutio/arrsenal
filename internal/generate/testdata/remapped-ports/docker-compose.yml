# GENERATED BY ARRSENAL — DO NOT EDIT.
# This file is regenerated from /opt/arrsenal/arrsenal.yaml on every run;
# your edits here will be lost. Put customizations in
# docker-compose.override.yml (same directory) — Compose merges it
# automatically and Arrsenal never touches it.
name: arrsenal
services:
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent
    restart: unless-stopped
    networks:
    - arrsenal
    environment:
      PGID: ${PGID}
      PUID: ${PUID}
      TZ: ${TZ}
      UMASK: ${UMASK}
      WEBUI_PORT: "9091"
    ports:
    - "9091:9091"
    - "16881:6881"
    - "16881:6881/udp"
    volumes:
    - ${APPDATA}/qbittorrent:/config
    - ${DATA}/torrents:/data/torrents
  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    restart: unless-stopped
    networks:
    - arrsenal
    environment:
      PGID: ${PGID}
      PUID: ${PUID}
      TZ: ${TZ}
      UMASK: ${UMASK}
    ports:
    - "9989:8989"
    volumes:
    - ${APPDATA}/sonarr:/config
    - ${DATA}:/data
networks:
  arrsenal:
    driver: bridge
    name: arrsenal
