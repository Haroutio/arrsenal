// Package tui implements the Bubble Tea interface: app selection, settings,
// paths with the live mount table, and conflict-remap prompts. Model logic
// stays unit-testable; rendering stays thin.
package tui
